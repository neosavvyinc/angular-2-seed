import { RouterConfig } from '@angular/router';
import { SampleFormComponent } from './SampleForm.component';

export const sampleFormRoutes: RouterConfig = [
    { path: 'sampleForm',  component: SampleFormComponent }
];
