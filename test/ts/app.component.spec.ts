import {
    it,
    describe,
    expect
} from '@angular/core/testing';

// import { AppComponent } from '../../src/ts/app.component.ts';

describe('Dumb Test', () => {

    it('knows numbers', () => {
        expect(1).not.toBe(2);
    });

});